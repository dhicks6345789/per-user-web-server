# Compiled Go binaries.
adminPanel/adminPanel
sessionProxy/sessionProxy
sessionManager/sessionManager
www/wwwServer

# Custom rclone build output (the rclone source tree, the built binary and Go build dirs).
rclone-build/rclone
rclone-build/src/
rclone-build/tmp/
rclone-build/gocache/

# Maven build output.
guacAutoConnect/target/
guacAutoConnect/dependency-reduced-pom.xml
