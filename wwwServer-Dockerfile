FROM debian:latest
RUN apt-get update && apt-get install -y python3 && apt-get clean
COPY per-user-web-server/www/wwwServer /usr/local/bin/wwwServer
COPY per-user-web-server/www/runCGI.py /usr/local/bin/runCGI.py
RUN chmod a+x /usr/local/bin/runCGI.py
EXPOSE 8080
